import { ReplaceStringWithNaPipe } from './replace-string-with-na.pipe';

describe('ReplaceStringWithNaPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceStringWithNaPipe();
    expect(pipe).toBeTruthy();
  });
});
